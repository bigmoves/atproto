/**
 * Switches between the current UI and the redesigned `#/components-v2` tree.
 * Checked only at the pages layer (entry points and route files) — see
 * docs/superpowers/specs/2026-07-14-oauth-provider-ui-redesign-design.md.
 */
export const NEW_DESIGN_ENABLED = true
